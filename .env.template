# Root environment template (copy to .env if needed)

# Backend (Laravel)
APP_NAME="NIOS Analytics"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8100

DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=bta_dashboard
DB_USERNAME=postgres
DB_PASSWORD=password123

REDIS_HOST=redis

# Frontend (Vite)
VITE_API_BASE=http://localhost:8100
